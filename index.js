/**
 * @format
 */

import { AppRegistry } from 'react-native';

import { Main, List, State } from './src';

import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => List);
